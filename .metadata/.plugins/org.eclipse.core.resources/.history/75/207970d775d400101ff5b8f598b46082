package in.ramana;

import java.util.Iterator;
import java.util.LinkedHashSet;

public class Q23 {
	public static void main(String[] args) {
		int [] arr = {1,2,1,3,2,4};
		LinkedHashSet<Integer> lhs = new LinkedHashSet<>();
		for(int s: arr) {
			lhs.add(s);
		}
		/*
		 * Iterator<Integer> it = lhs.iterator(); while(it.hasNext()) { Integer num =
		 * it.next(); System.out.print(num +" ") ; }
		 */
		for(int sunny : arr) {
			System.out.println(sunny);
		}
		
	}

}
